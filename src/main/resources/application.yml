spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hotel
    username: root
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
server:
  context-path: /hotel